version: '3'

services:

  micropub:
    build: .
    container_name: micropub
    restart: always
    expose:
      - 3031

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "80:80"
